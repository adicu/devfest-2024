/* Returns an array of arrays of length 2 (left and right pages). */
export default function getWorkshopPages(
  data,
  updatePage = null,
  pageDictionary = null
) {
  return [[<div>Left Workshop Page 1</div>, <div>Right workshop page 1</div>]];
}
