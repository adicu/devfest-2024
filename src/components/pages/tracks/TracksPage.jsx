import styled from "@emotion/styled";

const HomePageLeft = (props) => {
  const Container = styled.div`
    height: 100%;
    width: 100%;
    user-select: none;
  `;

  return <Container>Tracks Page</Container>;
};

export default HomePageLeft;
