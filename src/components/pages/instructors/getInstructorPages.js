/* Returns an array of arrays of length 2 (left and right pages). */
export default function getInstructorPages(
  data,
  updatePage = null,
  pageDictionary = null
) {
  return [
    [<div>Left Instructor Page 1</div>, <div>Right Instructor page 1</div>],
  ];
}
