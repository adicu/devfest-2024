/* Returns an array of arrays of length 2 (left and right pages). */
export default function getSponsorPages(
  data,
  updatePage = null,
  pageDictionary = null
) {
  return [[<div>Left Sponsor Page 1</div>, <div>Right Sponsor page 1</div>]];
}
