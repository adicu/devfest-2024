/* Returns an array of arrays of length 2 (left and right pages). */
export default function getSpeakerPages(
  data,
  updatePage = null,
  pageDictionary = null
) {
  return [[<div>Left Speaker Page 1</div>, <div>Right speaker page 1</div>]];
}
